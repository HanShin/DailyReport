@{
    ViewBag.Title = "ModelProgressPart";
}

<div class="col-xs-10 col-md-10">
    <h3>PipePart Progress</h3>
        @(Html.Kendo().Chart<DailyReport.Models.ModelCountData>()
        .Name("PartChart")
        .Title("Part 작업량")
        .Legend(legend => legend.Position(ChartLegendPosition.Bottom))
        .DataSource(ds => ds
            .Read(read => read
            .Action("ModelProgressPart_Chart", "Chart"))
        )
        .Series(series =>
        {
            series.Bar(model => model.Count).Name("Count").Labels(true);
        })
        .CategoryAxis(axis => axis
            .Categories(categories => categories.Date).Title("날짜")
        )
        .ValueAxis(axis => axis
            .Numeric()
            .Labels(labels => labels.Visible(true))
            .MajorUnit(100)

        )
        .Tooltip(tooltip => tooltip
            .Visible(true)
        )
        .ChartArea(chart => chart.Height(900))

)
</div>

